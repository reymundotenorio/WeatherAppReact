import React from 'react';

const Location = ({ city }) => (
  // Destructuring

  // console.log(props);
  // debugger;

  // const city = props.city;

  // const { city } = props; // Destructuring

  <div>
    <h1>{city}</h1>
  </div>
);

export default Location;
